# This rule is not used by the project operator-scaling-package-example itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants full permissions ('*') over webapp.com.tomludwig.
# This role is intended for users authorized to modify roles and bindings within the cluster,
# enabling them to delegate specific permissions to other users or groups as needed.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: operator-scaling-package-example
    app.kubernetes.io/managed-by: kustomize
  name: guestbook-admin-role
rules:
- apiGroups:
  - webapp.com.tomludwig
  resources:
  - guestbooks
  verbs:
  - '*'
- apiGroups:
  - webapp.com.tomludwig
  resources:
  - guestbooks/status
  verbs:
  - get
